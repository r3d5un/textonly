{{define "title"}}Post #{{.BlogPost.ID}}{{end}}
{{define "main"}}
    {{with .BlogPost}}
        <div class="row">
            <div class="container-fluid col-lg-5 mt-5">
                {{ markdownToHTML .Post }}
            </div>
        </div>
    {{end}}
{{end}}
